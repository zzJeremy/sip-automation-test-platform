# SIP自动化测试平台详细设计 - 基于planning-with-files原则

## 1. 三层架构设计 - 文件级任务规划

### 1.1 业务层（Business Layer）
- **功能**：业务逻辑编排、测试场景定义、高级测试用例组合
- **文件路径**: `business_layer/business_test_suite.py`
- **主要组件**：
  - BusinessTestSuite：业务测试套件，组织多个相关测试
  - EnhancedTestScenario：增强测试场景，支持复杂业务场景定义
  - TestScenarioManager：测试场景管理器，编排复杂测试流程
- **实现细节**：
  - 定义业务层面的测试场景
  - 组合多个基础测试形成复杂业务测试
  - 提供业务级别的测试报告
- **依赖**：客户端层组件、pytest执行器
- **文件依赖**：`business_layer/enhanced_test_scenario.py`, `core/pytest_integration/sip_dsl.py`
- **状态**：已实现
- **技术规范**：
  - 遵循pytest集成规范
  - 支持YAML测试用例格式
  - 提供链式调用API

#### 1.1.1 BusinessTestSuite设计
```python
import logging
from datetime import datetime
from typing import Dict, Any, List
from .enhanced_test_scenario import EnhancedTestScenario, TestScenarioManager


class BusinessTestSuite:
    def __init__(self, suite_name: str, description: str = ""):
        """初始化业务测试套件"""
        self.suite_name = suite_name
        self.description = description
        self.test_scenarios = []
        self.logger = logging.getLogger(__name__)
        
        # 创建场景管理器
        self.scenario_manager = TestScenarioManager()
        
    def add_scenario(self, scenario: EnhancedTestScenario):
        """添加测试场景到套件"""
        self.test_scenarios.append(scenario)
        self.scenario_manager.add_scenario(scenario)
        
    def execute_suite(self, config: Dict[str, Any]) -> Dict[str, Any]:
        """执行整个测试套件"""
        results = {
            "suite_name": self.suite_name,
            "description": self.description,
            "start_time": datetime.now(),
            "results": [],
            "overall_status": "PASS",
            "total_scenarios": len(self.test_scenarios),
            "successful_scenarios": 0,
            "failed_scenarios": 0
        }
        
        # 执行所有场景
        scenario_results = self.scenario_manager.execute_all_scenarios()
        results["results"] = scenario_results
        
        # 统计结果
        successful_scenarios = 0
        failed_scenarios = 0
        
        for result in scenario_results:
            if result.get("status") == "passed":
                successful_scenarios += 1
            else:
                failed_scenarios += 1
                if results["overall_status"] != "ERROR":
                    results["overall_status"] = "FAIL"
        
        results["successful_scenarios"] = successful_scenarios
        results["failed_scenarios"] = failed_scenarios
        results["end_time"] = datetime.now()
        results["duration"] = (results["end_time"] - results["start_time"]).total_seconds()
        
        return results
```

#### 1.1.2 EnhancedTestScenario设计
```python
import logging
from typing import Dict, Any, List, Optional
from datetime import datetime
from enum import Enum

from ..sip_client import ClientSelectionStrategy, TestRequirement, SIPClientBase
from ..sip_client.client_manager import SIPClientManager


class ScenarioStatus(Enum):
    """测试场景状态"""
    PENDING = "pending"
    RUNNING = "running"
    PASSED = "passed"
    FAILED = "failed"
    ERROR = "error"


class EnhancedTestScenario:
    """
    增强的测试场景管理器
    支持复杂业务场景定义和客户端自动选择
    """
    
    def __init__(self, name: str, description: str = "", requirement: TestRequirement = None):
        """
        初始化测试场景
        
        Args:
            name: 场景名称
            description: 场景描述
            requirement: 测试需求类型，用于客户端选择
        """
        self.name = name
        self.description = description
        self.requirement = requirement or TestRequirement.BASIC_SIP_PROTOCOL
        self.steps: List[Dict[str, Any]] = []
        self.status = ScenarioStatus.PENDING
        self.results: List[Dict[str, Any]] = []
        self.start_time: Optional[datetime] = None
        self.end_time: Optional[datetime] = None
        self.logger = logging.getLogger(__name__)
        
        # 客户端选择策略
        self.client_strategy: Optional[ClientSelectionStrategy] = None
        self.client: Optional[SIPClientBase] = None
```
        return results
```

#### 1.1.2 TestScenarioManager设计
```python
import logging
from typing import Dict, Any, List, Optional
from datetime import datetime
from enum import Enum

from ..sip_client import ClientSelectionStrategy, TestRequirement, SIPClientBase
from ..sip_client.client_manager import SIPClientManager


class TestScenarioManager:
    """
    测试场景管理器
    管理多个测试场景的执行
    """
    
    def __init__(self, config: Dict[str, Any] = None):
        """
        初始化场景管理器
        
        Args:
            config: 配置参数
        """
        self.config = config or {}
        self.scenarios: List[EnhancedTestScenario] = []
        self.client_strategy = ClientSelectionStrategy(self.config)
        self.logger = logging.getLogger(__name__)
    
    def add_scenario(self, scenario: EnhancedTestScenario):
        """
        添加测试场景
        
        Args:
            scenario: 测试场景
        """
        scenario.set_client_strategy(self.client_strategy)
        self.scenarios.append(scenario)
    
    def execute_all_scenarios(self) -> List[Dict[str, Any]]:
        """
        执行所有测试场景
        
        Returns:
            List: 所有场景的执行结果
        """
        results = []
        
        for scenario in self.scenarios:
            self.logger.info(f"开始执行场景: {scenario.name}")
            result = scenario.execute(self.config)
            results.append(result)
        
        return results
    
    def execute_scenario_by_name(self, name: str) -> Optional[Dict[str, Any]]:
        """
        按名称执行特定场景
        
        Args:
            name: 场景名称
            
        Returns:
            Optional[Dict]: 场景执行结果，如果未找到则返回None
        """
        for scenario in self.scenarios:
            if scenario.name == name:
                return scenario.execute(self.config)
        return None
```

### 1.2 SIP DSL模块（SIP Domain Specific Language）
- **功能**：提供简洁的API来定义SIP测试场景，支持流式调用风格
- **主要组件**：
  - SIPCallFlow：SIP呼叫流程DSL，支持链式调用
  - SIPMessageValidator：SIP消息验证器，提供灵活的消息匹配和验证功能
  - SIPTestScenario：SIP测试场景定义类
- **实现细节**：
  - 使用流式API设计模式
  - 支持SIP协议状态跟踪
  - 提供灵活的消息验证机制
- **依赖**：SIP客户端层、核心协议库

#### 1.2.1 SIPCallFlow设计
```python
import logging
from typing import Dict, Any, List
from enum import Enum
import time


class SIPMethod(str, Enum):
    """SIP方法枚举"""
    INVITE = "INVITE"
    ACK = "ACK"
    BYE = "BYE"
    CANCEL = "CANCEL"
    OPTIONS = "OPTIONS"
    REGISTER = "REGISTER"
    MESSAGE = "MESSAGE"


class SIPCallState(str, Enum):
    """SIP呼叫状态"""
    IDLE = "idle"
    CALLING = "calling"
    RINGING = "ringing"
    CONNECTED = "connected"
    TERMINATED = "terminated"


class SIPCallFlow:
    """SIP呼叫流程DSL类"""
    
    def __init__(self, caller_uri: str, callee_uri: str, client_manager=None):
        self.caller_uri = caller_uri
        self.callee_uri = callee_uri
        self.client_manager = client_manager
        self.steps = []
        self.expected_results = []
        self.call_id = f"call_{int(time.time())}"
        self.state = SIPCallState.IDLE
    
    def make_call(self, duration: int = 5):
        """发起呼叫"""
        self.steps.append({
            'action': 'make_call',
            'caller_uri': self.caller_uri,
            'callee_uri': self.callee_uri,
            'duration': duration,
            'call_id': self.call_id
        })
        self.expected_results.extend([
            f"成功发送INVITE到 {self.callee_uri}",
            f"收到200 OK响应",
            f"发送ACK确认",
            f"维持通话 {duration} 秒",
            f"发送BYE终止呼叫"
        ])
        self.state = SIPCallState.CALLING
        return self
    
    def wait_for_ringing(self):
        """等待振铃"""
        self.steps.append({
            'action': 'wait_for_response',
            'expected_code': 180,  # Ringing
            'call_id': self.call_id
        })
        self.expected_results.append("收到180 Ringing响应")
        self.state = SIPCallState.RINGING
        return self
    
    def wait_for_answer(self):
        """等待接听"""
        self.steps.append({
            'action': 'wait_for_response',
            'expected_code': 200,  # OK
            'call_id': self.call_id
        })
        self.expected_results.append("收到200 OK响应，呼叫建立成功")
        self.state = SIPCallState.CONNECTED
        return self
    
    def hold_call(self):
        """保持呼叫"""
        self.steps.append({
            'action': 'hold_call',
            'call_id': self.call_id
        })
        self.expected_results.append("成功发送保持请求")
        return self
    
    def unhold_call(self):
        """取消保持"""
        self.steps.append({
            'action': 'unhold_call',
            'call_id': self.call_id
        })
        self.expected_results.append("成功发送取消保持请求")
        return self
    
    def terminate_call(self):
        """终止呼叫"""
        self.steps.append({
            'action': 'send_bye',
            'call_id': self.call_id
        })
        self.expected_results.append("成功发送BYE请求")
        self.expected_results.append("收到200 OK响应，呼叫终止")
        self.state = SIPCallState.TERMINATED
        return self
    
    def reject_call(self):
        """拒绝呼叫"""
        self.steps.append({
            'action': 'send_reject',
            'call_id': self.call_id
        })
        self.expected_results.append("成功发送拒绝响应")
        self.state = SIPCallState.TERMINATED
        return self
    
    def wait(self, seconds: int):
        """等待指定秒数"""
        self.steps.append({
            'action': 'wait',
            'duration': seconds
        })
        return self
```

### 1.3 客户端层（Client Layer）
- **功能**：SIP客户端管理、协议通信、状态维护
- **主要组件**：
  - SIPClientBase：SIP客户端基类，定义通用接口
  - HybridClient：混合客户端，支持多种SIP实现
  - ClientManager：客户端管理器，统一管理客户端生命周期
  - DialogStateManager：对话状态管理器，跟踪SIP对话状态
  - ClientSelectionStrategy：客户端选择策略，根据测试需求自动选择客户端
- **实现细节**：
  - 封装底层SIP协议通信
  - 管理客户端连接和会话状态
  - 提供统一的SIP操作接口
  - 根据测试需求自动选择最合适的客户端实现
- **依赖**：核心协议库、网络层

### 1.3 核心层（Core Layer）- 基于pytest
- **功能**：测试执行、结果收集、报告生成
- **主要组件**：
  - pytest测试套件：使用pytest框架执行测试
  - Fixtures模块：提供测试资源管理
  - 报告生成器：生成多格式测试报告
- **实现细节**：
  - 利用pytest强大的测试发现和执行机制
  - 通过fixtures管理测试资源
  - 生成标准化测试报告
- **依赖**：pytest框架、业务层和客户端层

#### 1.3.1 pytest测试套件（pytest_sip_tests）

#### 1.3.1 Fixtures模块设计
```python
"""
SIP测试框架的pytest fixtures
提供SIP客户端、端口池、测试环境等共享资源
"""
import pytest
import tempfile
import os
import time
from typing import Dict, Any, Generator
from pathlib import Path

from AutoTestForUG.basic_sip_call_tester import BasicSIPCallTester
from AutoTestForUG.port_manager import PortManager
from AutoTestForUG.error_handler import SIPTestLogger


@pytest.fixture(scope="session")
def port_pool():
    """全局端口池，用于分配SIP测试端口"""
    port_manager = PortManager(start_port=5080, end_port=5100)
    yield port_manager
    # 清理端口资源
    port_manager.release_all()


@pytest.fixture
def sip_client_factory(port_pool):
    """SIP客户端工厂fixture"""
    created_clients = []
    
    def _create_client(
        server_host: str = "127.0.0.1",
        server_port: int = 5060,
        local_host: str = "127.0.0.1",
        local_port: int = None,
        client_id: str = None
    ):
        if local_port is None:
            local_port = port_pool.get_available_port()
        
        client = BasicSIPCallTester(
            server_host=server_host,
            server_port=server_port,
            local_host=local_host,
            local_port=local_port,
            client_id=client_id
        )
        created_clients.append(client)
        return client
    
    yield _create_client
    
    # 清理所有创建的客户端
    for client in created_clients:
        try:
            client.close()
        except:
            pass
```

#### 1.3.2 SIP DSL模块设计
```python
"""
SIP测试DSL（领域特定语言）
提供简洁的API来定义SIP测试场景
"""
import logging
from typing import Dict, Any, Callable, List
from dataclasses import dataclass
from enum import Enum
import time
import re


class SIPMethod(str, Enum):
    """SIP方法枚举"""
    INVITE = "INVITE"
    ACK = "ACK"
    BYE = "BYE"
    CANCEL = "CANCEL"
    OPTIONS = "OPTIONS"
    REGISTER = "REGISTER"
    MESSAGE = "MESSAGE"


class SIPCallState(str, Enum):
    """SIP呼叫状态"""
    IDLE = "idle"
    CALLING = "calling"
    RINGING = "ringing"
    CONNECTED = "connected"
    TERMINATED = "terminated"


@dataclass
class SIPMessage:
    """SIP消息数据类"""
    method: SIPMethod
    uri: str
    headers: Dict[str, str]
    body: str = ""
    call_id: str = ""
    cseq: int = 1


class SIPCallFlow:
    """SIP呼叫流程DSL类"""
    
    def __init__(self, caller_uri: str, callee_uri: str, client_manager=None):
        self.caller_uri = caller_uri
        self.callee_uri = callee_uri
        self.client_manager = client_manager
        self.steps = []
        self.expected_results = []
        self.call_id = f"call_{int(time.time())}"
        self.state = SIPCallState.IDLE
    
    def make_call(self, duration: int = 5):
        """发起呼叫"""
        self.steps.append({
            'action': 'make_call',
            'caller_uri': self.caller_uri,
            'callee_uri': self.callee_uri,
            'duration': duration,
            'call_id': self.call_id
        })
        self.expected_results.extend([
            f"成功发送INVITE到 {self.callee_uri}",
            f"收到200 OK响应",
            f"发送ACK确认",
            f"维持通话 {duration} 秒",
            f"发送BYE终止呼叫"
        ])
        self.state = SIPCallState.CALLING
        return self
    
    def wait_for_ringing(self):
        """等待振铃"""
        self.steps.append({
            'action': 'wait_for_response',
            'expected_code': 180,  # Ringing
            'call_id': self.call_id
        })
        self.expected_results.append("收到180 Ringing响应")
        self.state = SIPCallState.RINGING
        return self
    
    def wait_for_answer(self):
        """等待接听"""
        self.steps.append({
            'action': 'wait_for_response',
            'expected_code': 200,  # OK
            'call_id': self.call_id
        })
        self.expected_results.append("收到200 OK响应，呼叫建立成功")
        self.state = SIPCallState.CONNECTED
        return self
    
    def hold_call(self):
        """保持呼叫"""
        self.steps.append({
            'action': 'hold_call',
            'call_id': self.call_id
        })
        self.expected_results.append("成功发送保持请求")
        return self
    
    def unhold_call(self):
        """取消保持"""
        self.steps.append({
            'action': 'unhold_call',
            'call_id': self.call_id
        })
        self.expected_results.append("成功发送取消保持请求")
        return self
    
    def terminate_call(self):
        """终止呼叫"""
        self.steps.append({
            'action': 'send_bye',
            'call_id': self.call_id
        })
        self.expected_results.append("成功发送BYE请求")
        self.expected_results.append("收到200 OK响应，呼叫终止")
        self.state = SIPCallState.TERMINATED
        return self
    
    def reject_call(self):
        """拒绝呼叫"""
        self.steps.append({
            'action': 'send_reject',
            'call_id': self.call_id
        })
        self.expected_results.append("成功发送拒绝响应")
        self.state = SIPCallState.TERMINATED
        return self
    
    def wait(self, seconds: int):
        """等待指定秒数"""
        self.steps.append({
            'action': 'wait',
            'duration': seconds
        })
        return self
```
    clients = []
    
    def _create_client(client_id: str, local_port: int = None):
        port = local_port or port_pool.acquire()
        client = SIPClient(client_id=client_id, local_port=port)
        clients.append(client)
        return client
    
    yield _create_client
    
    # 清理所有客户端
    for client in clients:
        try:
            client.close()
        except:
            pass
    for client in clients:
        port_pool.release(client.local_port)
```

#### 1.1.2 conftest.py配置
- 定义pytest标记（markers）
- 配置默认测试选项
- 设置全局fixture

### 1.2 领域特定语言层（DSL）

#### 1.2.1 SIPCallFlow DSL设计
```python
class SIPCallFlow:
    def __init__(self, caller_uri: str, callee_uri: str):
        """初始化SIP呼叫流程"""
        self.caller_uri = caller_uri
        self.callee_uri = callee_uri
        self.steps = []
        self.duration = 0
        
    def invite(self):
        """添加INVITE步骤"""
        self.steps.append({"action": "invite", "timestamp": len(self.steps)})
        return self
        
    def wait_for_200_ok(self):
        """添加等待200 OK步骤"""
        self.steps.append({"action": "wait_200_ok", "timestamp": len(self.steps)})
        return self
        
    def ack(self):
        """添加ACK步骤"""
        self.steps.append({"action": "ack", "timestamp": len(self.steps)})
        return self
        
    def wait(self, seconds: int):
        """添加等待步骤"""
        self.duration += seconds
        self.steps.append({"action": "wait", "seconds": seconds, "timestamp": len(self.steps)})
        return self
        
    def bye(self):
        """添加BYE步骤"""
        self.steps.append({"action": "bye", "timestamp": len(self.steps)})
        return self
```

#### 1.2.2 SIPMessageValidator设计
```python
class SIPMessageValidator:
    def __init__(self):
        """初始化SIP消息验证器"""
        
    def validate_response_code(self, message: str, expected_code: int) -> bool:
        """验证响应码"""
        # 实现响应码验证逻辑
        pass
        
    def validate_header(self, message: str, header_name: str, expected_value: str) -> bool:
        """验证消息头字段"""
        # 实现头字段验证逻辑
        pass
        
    def validate_message_format(self, message: str) -> bool:
        """验证消息格式"""
        # 实现格式验证逻辑
        pass
        
    def extract_call_id(self, message: str) -> str:
        """提取Call-ID"""
        # 实现Call-ID提取逻辑
        pass
```

### 1.3 资源管理层

#### 1.3.1 PortPool设计
```python
class PortPool:
    def __init__(self, start_port: int, end_port: int):
        """初始化端口池"""
        self.start_port = start_port
        self.end_port = end_port
        self.available_ports = set(range(start_port, end_port + 1))
        self.allocated_ports = set()
        self.lock = threading.Lock()
        
    def acquire(self) -> int:
        """获取可用端口"""
        with self.lock:
            if not self.available_ports:
                raise Exception("No available ports")
            port = self.available_ports.pop()
            self.allocated_ports.add(port)
            return port
            
    def release(self, port: int) -> bool:
        """释放端口"""
        with self.lock:
            if port in self.allocated_ports:
                self.allocated_ports.remove(port)
                self.available_ports.add(port)
                return True
            return False
            
    def cleanup(self):
        """清理所有分配的端口"""
        with self.lock:
            self.available_ports.update(self.allocated_ports)
            self.allocated_ports.clear()
```

#### 1.3.2 TestEnvironment设计
```python
class TestEnvironment:
    def __init__(self, mode: str = "real"):
        """
        初始化测试环境
        mode: 'real' - 真实环境, 'mock' - 模拟环境
        """
        self.mode = mode
        self.resources = {}
        
    def setup(self):
        """设置测试环境"""
        if self.mode == "real":
            # 真实环境设置
            pass
        elif self.mode == "mock":
            # 模拟环境设置
            pass
            
    def teardown(self):
        """清理测试环境"""
        # 清理所有资源
        pass
```

## 2. 核心服务模块设计

### 2.1 Test Case Parser
- **功能**：解析YAML/JSON格式的测试用例定义
- **实现细节**：
  - 支持多种测试场景定义格式
  - 验证测试用例格式正确性
  - 将测试用例转换为pytest参数化输入
- **依赖**：PyYAML, json模块

#### 2.1.1 类设计
```python
class TestCaseParser:
    def __init__(self):
        """初始化测试用例解析器"""
        
    def parse_yaml(self, yaml_file: str) -> List[Dict]:
        """解析YAML格式测试用例"""
        with open(yaml_file, 'r', encoding='utf-8') as f:
            data = yaml.safe_load(f)
        return self._transform_to_pytest_format(data)
        
    def parse_json(self, json_file: str) -> List[Dict]:
        """解析JSON格式测试用例"""
        with open(json_file, 'r', encoding='utf-8') as f:
            data = json.load(f)
        return self._transform_to_pytest_format(data)
        
    def _transform_to_pytest_format(self, data: Dict) -> List[Dict]:
        """将测试用例数据转换为pytest参数化格式"""
        # 实现转换逻辑
        pass
```

### 2.2 Report Generator
- **功能**：生成多格式测试报告（JSON, HTML, Text）
- **实现细节**：
  - 收集pytest执行结果
  - 生成标准化报告格式
  - 支持自定义报告模板
- **依赖**：pytest内置报告功能

#### 2.2.1 类设计
```python
class ReportGenerator:
    def __init__(self):
        """初始化报告生成器"""
        
    def generate_json_report(self, results: List[Dict], output_file: str):
        """生成JSON格式报告"""
        report_data = self._format_results(results, "json")
        with open(output_file, 'w', encoding='utf-8') as f:
            json.dump(report_data, f, indent=2, ensure_ascii=False)
            
    def generate_html_report(self, results: List[Dict], output_file: str):
        """生成HTML格式报告"""
        # 使用模板生成HTML报告
        pass
        
    def generate_text_report(self, results: List[Dict], output_file: str):
        """生成文本格式报告"""
        # 生成人类可读的文本报告
        pass
        
    def _format_results(self, results: List[Dict], format_type: str) -> Dict:
        """格式化测试结果"""
        # 实现结果格式化逻辑
        pass
```

### 2.3 Robot Framework 适配器
- **功能**：提供Robot Framework到pytest的桥接功能，让非开发人员可以使用关键字驱动测试
- **实现细节**：
  - 将Robot Framework关键字映射到pytest测试
  - 支持RF格式测试用例转换为pytest执行
  - 提供统一的测试执行和报告接口
- **依赖**：robotframework, pytest, subprocess

#### 2.3.1 RobotFrameworkAdapter设计
```python
class RobotFrameworkAdapter:
    """
    Robot Framework 适配器
    将Robot Framework测试转换为pytest执行
    """
    
    def __init__(self):
        self.keyword_mapping = {
            # SIP基本操作关键字
            'Register User': 'register_user',
            'Unregister User': 'unregister_user',
            'Make SIP Call': 'make_sip_call',
            'End Call': 'end_call',
            'Send SIP Message': 'send_sip_message',
            'Verify Call Status': 'verify_call_status',
            
            # 测试断言关键字
            'Should Be Equal': 'should_be_equal',
            'Should Contain': 'should_contain',
            'Should Match Regexp': 'should_match_regexp',
            
            # 通用关键字
            'Log': 'log_message',
            'Sleep': 'sleep',
            'Set Variable': 'set_variable',
        }
        
    def rf_to_pytest_keywords(self, rf_keyword: str) -> str:
        """将Robot Framework关键字转换为pytest关键字"""
        return self.keyword_mapping.get(rf_keyword, rf_keyword.lower().replace(' ', '_'))
    
    def convert_rf_test_to_pytest(self, rf_test_content: str) -> str:
        """
        将Robot Framework测试转换为pytest测试
        """
        pass
        
    def execute_rf_style_test(self, rf_test_file: str) -> Dict[str, Any]:
        """
        执行Robot Framework风格的测试文件
        通过转换为pytest后执行
        """
        pass

class SIPRobotKeywords:
    """
    SIP相关的Robot Framework关键字
    这些关键字会被映射到底层的pytest/SIP实现
    """
    
    def __init__(self):
        from AutoTestForUG.sip_client.hybrid_client import HybridClient
        self.client = HybridClient()
    
    def register_user(self, username: str, password: str, server_uri: str) -> bool:
        """注册SIP用户"""
        pass
    
    def unregister_user(self, username: str) -> bool:
        """注销SIP用户"""
        pass
    
    def make_sip_call(self, caller_uri: str, callee_uri: str, duration: int = 30) -> bool:
        """发起SIP呼叫"""
        pass
    
    def end_call(self) -> bool:
        """结束当前呼叫"""
        pass
    
    def send_sip_message(self, sender_uri: str, recipient_uri: str, message: str) -> bool:
        """发送SIP消息"""
        pass
    
    def verify_call_status(self, expected_status: str) -> bool:
        """验证呼叫状态"""
        pass
```

### 2.4 Test Executor
- **功能**：测试执行器，集成pytest运行并提供命令行接口，支持Robot Framework和pytest格式的测试
- **实现细节**：
  - 解析命令行参数
  - 根据测试文件类型选择执行方式（直接pytest或通过RF适配器）
  - 处理执行结果
- **依赖**：argparse, subprocess, pytest, RobotFrameworkAdapter

#### 2.4.1 类设计
```python
class TestExecutor:
    """
    统一测试执行器
    支持执行pytest和Robot Framework格式的SIP测试用例
    """
    def __init__(self, config: Dict[str, Any]):
        self.config = config
        self.rf_adapter = RobotFrameworkAdapter()
        self.logger = logging.getLogger(__name__)
    
    def execute_tests(self, test_path: str, test_format: str = "pytest") -> Dict[str, Any]:
        """
        执行测试，支持多种格式
        """
        if test_format.lower() == "robot" or test_path.endswith(".robot"):
            return self._execute_robot_tests(test_path)
        else:
            return self._execute_pytest_suite(test_path)
    
    def _execute_robot_tests(self, robot_file: str) -> Dict[str, Any]:
        """
        执行Robot Framework格式的测试
        """
        return self.rf_adapter.execute_rf_style_test(robot_file)
    
    def _execute_pytest_suite(self, pytest_module: str) -> Dict[str, Any]:
        """
        执行pytest测试套件
        """
        pass
```

## 3. 数据结构设计

### 3.1 SIP消息结构
```python
class SIPMessage:
    def __init__(self, method: str = None, uri: str = None, headers: dict = None, body: str = None):
        self.method = method
        self.uri = uri
        self.headers = headers or {}
        self.body = body
        self.timestamp = time.time()
        self.call_id = self._extract_call_id()
        
    def _extract_call_id(self) -> str:
        """从头部提取Call-ID"""
        return self.headers.get('Call-ID', '')
```

### 3.2 测试结果结构
```python
class TestResult:
    def __init__(self):
        self.test_id = ""
        self.test_name = ""
        self.test_description = ""
        self.start_time = None
        self.end_time = None
        self.duration = 0
        self.status = "PENDING"  # PENDING, RUNNING, PASSED, FAILED, ERROR, SKIPPED
        self.message = ""
        self.traceback = ""  # 错误堆栈信息
        self.sip_messages = []  # SIP消息追踪
        self.pcaps = []  # 网络包捕获
        self.metrics = {}  # 性能指标
```

### 3.3 YAML测试用例结构
```yaml
test_cases:
  - name: "基础呼叫建立测试"
    description: "测试基本的SIP呼叫建立流程"
    markers: ["sip_basic", "regression"]  # pytest标记
    config:
      server_host: "127.0.0.1"
      server_port: 5060
      transport: "UDP"
      timeout: 30
    parameters:
      - caller_uri: "sip:alice@127.0.0.1:5060"
        callee_uri: "sip:bob@127.0.0.1:5060"
        call_duration: 5
      - caller_uri: "sip:charlie@127.0.0.1:5060"
        callee_uri: "sip:diana@127.0.0.1:5060"
        call_duration: 3
    steps:
      - action: "send_invite"
        description: "发送INVITE请求"
      - action: "wait_for_200ok"
        description: "等待200 OK响应"
      - action: "send_ack"
        description: "发送ACK确认"
      - action: "send_bye"
        description: "发送BYE请求终止呼叫"
```

## 4. 错误处理机制

### 4.1 错误分类
- **SIP协议错误**：SIP消息格式错误、状态码错误
- **网络错误**：连接超时、网络不可达
- **资源错误**：端口占用、资源不足
- **配置错误**：参数错误、配置缺失
- **执行错误**：pytest执行失败、测试异常

### 4.2 异常处理策略
- 统一异常基类定义
- pytest异常集成处理
- SIP特定错误处理
- 资源清理保障机制

## 5. 性能优化策略

### 5.1 资源管理
- 端口池复用管理
- 客户端实例复用
- pytest fixtures生命周期管理

### 5.2 并发处理
- pytest-xdist并行执行
- fixture作用域优化
- 资源隔离机制

## 6. 安全设计

### 6.1 配置安全
- 敏感信息保护
- 配置文件权限控制

### 6.2 执行安全
- 沙箱执行环境
- 权限最小化原则

## 7. 测试策略

### 7.1 单元测试
- DSL组件单元测试
- Fixtures功能测试
- 解析器准确性测试

### 7.2 集成测试
- pytest执行流程测试
- 报告生成测试
- 命令行接口测试

### 7.3 端到端测试
- 完整测试流程验证
- 多格式测试用例验证

## 8. 部署与运维设计

### 8.1 部署配置
- Python依赖管理
- 环境变量配置
- 命令行工具安装

### 8.2 监控与日志
- pytest内置日志集成
- SIP消息追踪记录
- 性能指标收集

### 8.3 维护与升级
- 版本兼容性管理
- DSL API稳定性保证
- 向后兼容性策略

### 1.4 前端界面层（Frontend Interface Layer）
- **功能**：提供现代化Web界面，支持测试套件管理、执行监控、报告查看等功能
- **主要组件**：
  - SuiteManagement.vue：测试套件管理界面，提供可视化界面管理测试套件
  - ExecutionMonitor.vue：执行监控界面，实时展示测试执行状态
  - ReportCenter.vue：报告中心界面，展示测试报告和统计信息
  - ScenarioEditor.vue：场景编辑器，提供拖拽式场景编排功能
  - Dashboard.vue：仪表板界面，展示系统概览和统计信息
  - api.js：API工具模块，封装前后端通信接口
  - websocket.js：WebSocket通信模块，实现实时数据推送
  - main.js：状态管理模块，使用Pinia管理应用状态
- **实现细节**：
  - 使用Vue 3 + Element Plus构建现代化界面
  - 通过REST API与后端通信
  - 通过WebSocket实现实时监控和日志推送
  - 使用Pinia进行状态管理
  - 支持拖拽式场景编排
  - 提供多格式报告导出功能
- **依赖**：后端API服务、WebSocket服务

#### 1.4.1 SuiteManagement.vue设计
```javascript
// SuiteManagement.vue - 测试套件管理界面
<template>
  <div class="suite-management-container">
    <div class="page-header">
      <h2>测试套件管理</h2>
      <el-button type="primary" @click="openCreateDialog">新建套件</el-button>
    </div>

    <el-card class="search-card">
      <el-form :model="searchForm" inline>
        <el-form-item label="套件名称">
          <el-input v-model="searchForm.name" placeholder="请输入套件名称" />
        </el-form-item>
        <el-form-item>
          <el-button type="primary" @click="searchSuites">搜索</el-button>
          <el-button @click="resetSearch">重置</el-button>
        </el-form-item>
      </el-form>
    </el-card>

    <el-table 
      :data="suites" 
      v-loading="loading"
      style="width: 100%"
      row-key="id"
    >
      <el-table-column prop="name" label="套件名称" width="200" />
      <el-table-column prop="description" label="描述" />
      <el-table-column prop="scenarioCount" label="场景数" width="100" />
      <el-table-column prop="lastStatus" label="最后执行状态" width="120">
        <template #default="{ row }">
          <el-tag :type="getStatusType(row.lastStatus)">{{ row.lastStatus }}</el-tag>
        </template>
      </el-table-column>
      <el-table-column prop="lastRunTime" label="最后执行时间" width="180" />
      <el-table-column label="操作" width="220">
        <template #default="{ row }">
          <el-button size="small" @click="runSuite(row)">运行</el-button>
          <el-button size="small" @click="editSuite(row)">编辑</el-button>
          <el-button size="small" type="danger" @click="deleteSuite(row)">删除</el-button>
        </template>
      </el-table-column>
    </el-table>
  </div>
</template>

<script>
import { ref, reactive, onMounted } from 'vue'
import { ElMessage } from 'element-plus'
import { suiteApi } from '@/utils/api'

export default {
  name: 'SuiteManagement',
  setup() {
    // 响应式数据
    const suites = ref([])
    const loading = ref(false)
    const searchForm = reactive({
      name: ''
    })

    // 获取测试套件列表
    const fetchSuites = async () => {
      loading.value = true
      try {
        const data = await suiteApi.getAllSuites(searchForm)
        suites.value = data.items || data
      } catch (error) {
        ElMessage.error('获取测试套件失败: ' + error.message)
      } finally {
        loading.value = false
      }
    }

    onMounted(() => {
      fetchSuites()
    })

    return {
      suites,
      loading,
      searchForm,
      fetchSuites
    }
  }
}
</script>
```

#### 1.4.2 ExecutionMonitor.vue设计
```javascript
// ExecutionMonitor.vue - 执行监控界面
<template>
  <div class="execution-monitor-container">
    <div class="page-header">
      <h2>测试执行与监控</h2>
      <div class="header-actions">
        <el-button type="primary" @click="startExecution">开始执行</el-button>
        <el-button type="danger" @click="stopExecution" :disabled="!isExecuting">停止执行</el-button>
        <el-button @click="clearLogs">清空日志</el-button>
      </div>
    </div>

    <el-card class="control-panel">
      <el-form :model="executionForm" :inline="true" label-width="100px">
        <el-form-item label="选择套件">
          <el-select v-model="executionForm.selectedSuites" multiple placeholder="请选择测试套件" style="width: 300px;">
            <el-option 
              v-for="suite in testSuites" 
              :key="suite.id" 
              :label="suite.name" 
              :value="suite.id" 
            />
          </el-select>
        </el-form-item>
        <el-form-item label="并发数">
          <el-input-number v-model="executionForm.concurrentCount" :min="1" :max="10" />
        </el-form-item>
        <el-form-item label="环境">
          <el-select v-model="executionForm.environment" placeholder="请选择执行环境">
            <el-option label="开发环境" value="development" />
            <el-option label="测试环境" value="test" />
            <el-option label="生产环境" value="production" />
          </el-select>
        </el-form-item>
      </el-form>
    </el-card>

    <el-card class="execution-status">
      <div class="status-header">
        <div class="status-info">
          <div class="status-item">
            <span class="status-label">执行状态:</span>
            <el-tag :type="getStatusType(executionStatus)" size="large">{{ executionStatus }}</el-tag>
          </div>
          <div class="status-item">
            <span class="status-label">进度:</span>
            <el-progress 
              :percentage="executionProgress" 
              :status="getProgressStatus(executionStatus)"
              :stroke-width="20"
            />
          </div>
        </div>
      </div>
    </el-card>

    <el-row :gutter="20">
      <el-col :span="16">
        <el-card class="log-panel">
          <template #header>
            <div class="card-header">
              <span>实时执行日志</span>
            </div>
          </template>
          <div ref="logContainer" class="log-container">
            <div 
              v-for="(log, index) in executionLogs" 
              :key="index" 
              class="log-item"
              :class="getLogClass(log.level)"
            >
              <span class="log-time">[{{ log.time }}]</span>
              <span class="log-level">[{{ log.level }}]</span>
              <span class="log-message">{{ log.message }}</span>
            </div>
          </div>
        </el-card>
      </el-col>
    </el-row>
  </div>
</template>

<script>
import { ref, reactive, onMounted, onUnmounted, nextTick } from 'vue'
import { ElMessage } from 'element-plus'
import wsService from '@/utils/websocket'

export default {
  name: 'ExecutionMonitor',
  setup() {
    const logContainer = ref(null)
    
    // 执行表单
    const executionForm = reactive({
      selectedSuites: [],
      concurrentCount: 1,
      environment: 'test'
    })

    // 执行状态
    const executionStatus = ref('idle')
    const executionProgress = ref(0)
    const isExecuting = ref(false)

    // 执行日志
    const executionLogs = ref([])

    // 开始执行
    const startExecution = async () => {
      if (executionForm.selectedSuites.length === 0) {
        ElMessage.warning('请选择至少一个测试套件')
        return
      }

      try {
        // 调用API开始执行
        const executionData = {
          suiteIds: executionForm.selectedSuites,
          concurrentCount: executionForm.concurrentCount,
          environment: executionForm.environment
        }
        
        await executionApi.startExecution(executionData)
        isExecuting.value = true
        executionStatus.value = 'running'
        executionProgress.value = 0

        // 初始化WebSocket连接以接收实时日志
        wsService.connect(process.env.VUE_APP_WS_URL || 'ws://localhost:8080/ws/execution')
      } catch (error) {
        ElMessage.error(error.message || '执行开始失败')
      }
    }

    return {
      logContainer,
      executionForm,
      executionStatus,
      executionProgress,
      isExecuting,
      executionLogs,
      startExecution
    }
  }
}
</script>
```

## 9. 质量保证措施

### 9.1 代码质量
- pytest代码规范
- DSL API一致性
- 自动化测试覆盖率
- 前端代码规范（ESLint、Prettier）

### 9.2 文档规范
- DSL API文档
- 使用示例文档
- 配置参考文档
- 前端界面使用文档

### 9.3 持续集成
- pytest集成测试
- 多环境兼容性测试
- 代码质量门禁
- 前端构建和测试流水线